let shoppinglist = ['apple','blueberries']; // [] --> Array

console.log(shoppinglist);
console.log(shoppinglist[1]);

shoppinglist [10] = "pears";

console.log(shoppinglist);
console.log(shoppinglist.length);

shoppinglist.splice(4,1); // remove from one element, starting at index 4

console.log(shoppinglist);
console.log(shoppinglist.length);

shoppinglist.splice(2,7) // removes 7 elements, starting at index 2

console.log(shoppinglist);
console.log(shoppinglist.length);

shoppinglist[shoppinglist.length] = "lemon"; // adds a new element
shoppinglist[shoppinglist.length] = "lemon";
shoppinglist[shoppinglist.length] = "lemon";
shoppinglist[shoppinglist.length] = "lemon";

console.log(shoppinglist);
i = 1
while (i = 1){
    shoppinglist[shoppinglist.length] = "yo mama so fat when she gets in an elevator the only way she can go is down";
    y = Math.random();
    console.log(shoppinglist.length)
    if (y > .9999999){
        console.log(y)
        break;
    }
}